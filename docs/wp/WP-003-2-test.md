# WP-003-2-test: 手动测试验证

## 🤖 Subagent 读取指令

> **重要**: 执行此任务前，请先阅读父工作包文档获取完整上下文：
> - 父工作包: `docs/wp/WP-003.md`
> - 包含: 问题分析、完整实施计划、验收标准

## 基本信息

| 属性 | 值 |
|------|-----|
| **类型** | test |
| **父工作包** | WP-003 |
| **依赖** | WP-003-1-impl |
| **执行角色** | tester |
| **状态** | 📋 待执行 |

## 职责

对 WP-003-1-impl 的实现进行手动测试，覆盖所有新增功能和边界情况。

## 任务清单

- [ ] 测试 Add-Model 空行取消退出
  - 在每个输入步骤按 Enter，验证是否正确取消并返回主菜单
  - 验证取消后不影响已有配置
- [ ] 测试 Add-Model 完整参数输入
  - 验证 5 个参数（AUTH_TOKEN/BASE_URL/OPUS/SONNET/HAIKU）正确写入生成的 settings 文件
  - 验证默认值（BASE_URL/OPUS/SONNET/HAIKU）空行时取默认值
- [ ] 测试 Initialize-Config 参数输入
  - 删除配置文件后重新初始化，验证参数输入和模板生成
- [ ] 测试模板复制
  - 验证新生成的 settings 文件保留了模板中的自定义配置（hooks, permissions, plugins 等）
  - 测试无模板时的 fallback 行为
- [ ] 回归测试
  - 验证模型选择、项目选择、启动 Claude 等原有功能正常

## 验收标准

- [ ] Add-Model 每步可空行取消退出
- [ ] 5 个参数正确写入 settings
- [ ] 模板复制保留原有配置
- [ ] 原有功能不受影响
